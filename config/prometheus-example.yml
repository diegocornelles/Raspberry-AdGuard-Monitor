# Añadir esto al final de /etc/prometheus/prometheus.yml

scrape_configs:
  # ... otras configuraciones ...

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']

  - job_name: 'adguard'
    static_configs:
      - targets: ['localhost:9618']
